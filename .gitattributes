# 本项目为纯静态站点（婚礼/求婚页面），无 ML 模型文件，不需要 Git LFS。
# 以下规则来自魔搭 ML 模板，已移除以避免 LFS 对象校验阻塞 push 到魔搭。
# 如需恢复 LFS，可参考 ModelScope 官方模板重新添加。

# 图片/音频 (*.jpg/*.jpeg/*.png/*.mp3/*.mp4) 不走 LFS，直接作为普通 git 对象提交。
# 原因：两个部署 workflow（GitHub Pages / 魔搭同步）均未安装 git-lfs，LFS 指针会导致生产环境图片/音频损坏。
# 魔搭大包 push 的 "curl 18" 由 sync-to-modelscope.yml 的 http.postBuffer=500MB + 指数退避重试兜底，无需 LFS。
# 历史教训：曾因这里声明了 LFS 规则但未对存量文件做 migrate，导致被改动的图片提交成 132 字节指针且对象未推送，全站图片损坏。
